name: Build Arsalan VPN APK

on:
  push:
    branches:
      - vpn-app-build-20260924
    paths:
      - 'arsalan-vpn/**'
      - '.github/workflows/build-arsalan-vpn.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: arsalan-vpn
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Arsalan-VPN-APK
          path: arsalan-vpn/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
